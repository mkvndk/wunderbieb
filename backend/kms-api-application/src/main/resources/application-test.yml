spring:
  datasource:
    url: jdbc:h2:mem:wunderbieb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

kms:
  security:
    oidc-enabled: false
    debug-role-header-enabled: true
  storage:
    minio:
      endpoint: http://localhost:9000
      access-key: test-access
      secret-key: test-secret
